ALDUIN X — OPTIMIZED EDITION

Запуск: через локальный HTTP-сервер.

Что исправлено:
- Loader больше не ждёт IndexedDB, FPS, частицы или Local AI.
- Исправлена критическая ошибка запуска JavaScript.
- WebLLM загружается лениво только при нажатии «Запустить Local AI».
- Local AI (Phi-3.5-mini WebLLM + WebGPU) сохранён.
- Дневник, галерея IndexedDB, лор, календарь, хронология, профиль, настройки и экспорт/импорт сохранены.
- AI-вкладка исправлена и добавлена в мобильную навигацию.

Важно: первый запуск Local AI может быть долгим, потому что браузер скачивает модель. Это происходит только после запуска AI, а не при открытии сайта.
